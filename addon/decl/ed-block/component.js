/**
 * @module ember-declarative/decl/ed-block/component
 *
 * Wrapper for declarations. Currently, just makes sure
 * they are hidden.
 */

import Ember from 'ember';

export default Ember.Component.extend({
  isVisible: false,

});